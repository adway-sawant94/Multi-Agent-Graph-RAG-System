# Gemini API Configuration (Replace with your actual key)
GEMINI_API_KEY=

# Model Configuration
EMBEDDING_MODEL=models/gemini-embedding-001
LLM_MODEL=models/gemini-3.5-flash

# System Settings
# Set to True to test/run without an API key (offline mock mode)
USE_MOCK_FALLBACK=True
